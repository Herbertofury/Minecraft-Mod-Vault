# Forge run configuration request

Export the exact ForgeGradle 1.20.1 / Forge 47.4.23 `runServer` launch by actually starting the task under Java 17, capturing its JVM arguments/module path/classpath/system properties and fresh server log for local smoke verification of the ragdoll ports.

Request revision: 2.
