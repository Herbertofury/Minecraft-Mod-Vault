name: Build Explorer From Canonical Sheet

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  staging-maintenance:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Authenticated current workbook staging is in progress; publication resumes after exact byte verification.'
