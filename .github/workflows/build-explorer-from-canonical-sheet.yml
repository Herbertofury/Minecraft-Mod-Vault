name: Build Explorer From Canonical Sheet

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  staging-maintenance:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Explorer publication payload is being repaired; automatic push execution resumes after exact staging verification.'
